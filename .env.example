# Mint frontend env. Copy to .env.local and fill in.
# Until these are set, the app runs on mock data (see src/lib/supabase.ts).

# Supabase project (Mint org). Anon key only on the client, never the service role.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
